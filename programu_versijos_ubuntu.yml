---
- name: list_package
  hosts: all
  become: yes

  tasks:
  - name: Get packages
    shell: dpkg-query -W '${binary:Package}\n' -W
    register: packages

  - name: Print packages
    debug:
      msg: "{{ packages.stdout_lines }}"
